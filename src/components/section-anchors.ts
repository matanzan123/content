/**
 * Stable ids for same-page jumps, shared by the link and its target so the two
 * can never drift apart.
 *
 * The scroll offset that keeps a target's heading clear of the sticky header is
 * the `scroll-anchor-offset` class in globals.css — apply it alongside the id.
 */
export const VERIFIED_BRANDS_ID = "verified-brands";
